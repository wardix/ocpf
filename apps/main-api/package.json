{
  "name": "@omnichannel/main-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run migrate.ts up && bun run --watch src/index.ts",
    "build": "echo 'No build step required for main-api'",
    "start": "bun run migrate.ts up && bun run src/index.ts",
    "migrate:up": "bun run migrate.ts up",
    "migrate:down": "bun run migrate.ts down"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.6.1",
    "@hono/zod-validator": "^0.8.0",
    "@omnichannel/shared-types": "workspace:*",
    "@sentry/bun": "^8.9.0",
    "csv-stringify": "^6.7.0",
    "exceljs": "^4.4.0",
    "hono": "^4.0.0",
    "ioredis": "^5.3.0",
    "node-pg-migrate": "^8.0.4",
    "pg": "^8.21.0",
    "pino": "^10.3.1",
    "postgres": "^3.4.0",
    "prom-client": "^15.1.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}